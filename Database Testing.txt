Database ---> To store/to store Permanenetly 
Database Management System --- DBMS --- To manage database 
RDBMS --- Relational Database Management System  (Tables are related )
Tools --- MS-Access , MySql , SQL Server , Oracle,postgre ,  (SQL)

--Manipulation 
--retrival /fetch

Language ---SQL  (Structure Query Langauge)

---data access,
---change
--delete
--insert 
--permiss
--transection
--contrains 

SQL --- Types of statement 
1. DDL  (Data Definition Language)   --- relate to table (create,alter,drop)
2. DML  (Data manipulate Language)   ---related to data (insert , update,delete)
3. DQL  (Data Query Language)      ---it won't affect the data-retrival (select)
4. DCL  (Data Control Language)  ---  permission (grant,revoke)

Constraints ---- limitation/rules 
1. Primary key
2. forighn key
3. not null
4. unique key
5. check constraint

Database Oject 
--Tables 
---views
---Store procedure
---trigger
--functions

TABLES ---- row/colum---data
row ---data/record
column --- fields
Students
id,name,c_no,address,birthDate  (column)
1  Rohit 3434 CGRoad 2nd March 2000 --row/data
2  Tarun  233 Paldi  4th Apr 2000 ---row/data

13th March 
-create database college
  execute

database --tables 
          views
          stored procedures
          functions

tables ---
      filed Name/filed type
      student table  
            student name      characters----varchar
            rollnumber        numerics  --- int
            b'date            date  ---date
            address            varchar
            c_number           numerics --- int   
            email_address      varchar
            semester            numeric --- int  (4)
            fees                numeric ---decimal (12000.23)
  data types 
      which type of data you are storing ?
      numeric  --- decimal-non decimal 
      string --- char,varchar,text...
      date/time---date,datetime

TABLE create 

create table student(rollnumber int,sname varchar(20),b_date date,
address varchar(50),
email varchar(20),
c_no int,
fees decimal(7,2)
)

Alter Table--

table already created that we have to change
create table student(rollnumber int,sname varchar(20),b_date date,
address varchar(50),
email varchar(20),
c_no int,
fees decimal(7,2)
);

describe student;

alter table student add column semster int;

describe student;

16th March 

alter table student drop column email;

-- table drop
permanenet drop the table
drop table student;
--
DML
data insert --
insert into table_name(col2,col2,col3,col4) values(va1,val2,val3,val4);
insert into student(rollnumber,sname) value(101,'Yash')


insert into student(rollnumber,sname,email) value(101,'Yash','yash@gmail.com');
select * from student;

18th March 
--insert multiple records 
insert into student(rollnumber,sname,email) values(101,'Yash','yash@gmail.com'),
(201,"Rohit","rohit@gmail.com"),
(301,"Tarun","Tarun@gmail.com"),
(401,"Dharmishtha","dharmishtha@gmail.com")

constraints 
1. Primary key ---to uniquely identify records.
  ---not null + unique
table contains one primary key
eg. rollno,m_no,email,emp_id

2. unique key --- to uniquely identify record.
it allows one value as NULL
table contains more then one   unique key

3. Foreighn key ---
HR,
Finance
Sales 
Marketing
HR ke 4 employee
Sales -10 employees
Marketing 6 employee
----PK references to another table

4. Not null
Lab Task : Primary key vs Unique key vs Forighen key

create table student(r_no int primary key,
sname varchar(50) not null,
email varchar(20) unique key,
c_no int unique key)

--------

create table department(dept_id int primary key,dept_name varchar(20));


create table employee(emp_id int primary key,
emp_name varchar(20) not null,
email varchar(20) default 'test@gmail.com',
city varchar(20),
salary int,
did int,
foreign key(did) references department(dept_id)

);
